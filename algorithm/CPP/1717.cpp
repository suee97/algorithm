#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, m;
    cin >> n >> m;

    for (int i = 0; i < m; i++) {
        int command, a, b;
        cin >> command >> a >> b;
        if (command == 0) {
            
        }
    }
}
